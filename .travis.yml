sudo: required
services:
  - docker
install: false
jobs:
  include:
    - stage: continuous_integration
      if: NOT tag IS present
      script:
        - make prepare
        - make test
        - make build
    - stage: deployment
      if: tag IS present
      script:
        - make prepare
        - make test
        - make build
        - make publish
