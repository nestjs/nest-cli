const { Collection } = require('./collection');
const { CollectionFactory } = require('./collection.factory');
const { SchematicOption } = require('./schematic.option');

module.exports = {
  Collection,
  CollectionFactory,
  SchematicOption
};