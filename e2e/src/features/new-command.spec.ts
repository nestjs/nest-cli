describe('New Command Specifications', () => {
  it('should generate a new typescript Nest project', () => {
    // execute command
  });
  it('should generate a new Javascript Nest project', () => {
    // execute command
  });
});
